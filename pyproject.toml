[project]
name = "agentic-coding"
version = "0.1.0"
description = ""
authors = [
    {name = "Pradeep Kumar",email = "webreinvent@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "autogen-agentchat (>=0.5.7,<0.6.0)",
    "autogen-ext[docker] (>=0.5.7,<0.6.0)",
    "python-dotenv (>=0.13.0,<1.0.0)",
    "setuptools (>=80.9.0,<81.0.0)",
    "docker (>=7.1.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
